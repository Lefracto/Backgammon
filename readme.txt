Тут немного накидаю того, что хотелось бы уточнить, может обратить внимание:

- Немного теряет смысл GameServiceResponse, поскольку теперь ход делается по нажатию на поле, на котором отображается с большего только то, куда можно походить (ну или куда можно ходить кубиками, т.е. по числам кубиков и не занятости соперником, но даже так GameServiceResponse может значительно похудеть)

- Интерфейсы IPossibleMovesIndicator, IPossibleMovesProvider. Нормально в контексте используются? Есть ли смысл обьединять IPossibleMovesProvider с ITurnValidator?

- Методы GetPossibleMovesDouble и GetPossibleMovesNonDouble ещё немного недоработаны, там есть один баг в GetPossibleMovesDouble с расчётом кубиков для хода, но отображение работает корректно. Так же они довольно массивные, думаю вынесу ещё подметодов пару.

- Сейчас GameFieldView напрямую работает с CheckerView, прямая зависимость. Может вынести в интерфейс ICheckerView? И пока у их немного кривоватое взаимодействие, там переменые второстепенные какие-то, доп. проверки и т.д. Это поправлю, просто вопрос по интерфейсу.


Тут ещё хватает багов всяких багов, всё в процессе, просто хочется полировать после +- подтверждения, что не совсем фигню делаю)( 